server:
  port: ${APPLICATION_PORT:8080}

spring:
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}

grpc:
  server:
    port: 6565
    reflection-service-enabled: true

kafka:
  topics:
    commands: commands
    queries: queries
    responses: responses